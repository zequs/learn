### bootstrap 上下文配置
# 配置服务器 URI
spring.cloud.config.uri = http://localhost:9091/
# 配置客户端应用名称:{application}
spring.cloud.config.name = zequs
# profile 是激活配置
spring.cloud.config.profile = dev
# label 在Git中指的分支名称
spring.cloud.config.label = master 
